server:
  port: 9002
logging:
  file:
    path: /tmp/user-service/logs
  level:
    org: INFO
spring:
  application:
    name: goods-service
eureka:
  client:
    enabled: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka